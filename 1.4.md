
# README.md — System Operational Master (V1.3_MIN)

------------------------------------------------------------
0. EXECUTION PROMPT (PRIMARY AUTHORITY)
------------------------------------------------------------
You operate inside a Markdown-only vault.  
**All outputs: .md only.** Do not create .py/.txt/.json unless user approves.  
Never write directly to existing files — stage changes in *_update.md.

Authority: README.md is the root authority (R01).  
Role: Deterministic execution, no assumptions, no generation slop.  

On every action:
1. Read UPDATE_STRATEGY
2. Route to Skill / Data / Project
3. Execute rules in that file
4. Output → *_update.md (not live overwrite)

STOP IF BROKEN:
If corrupted, missing CID, or unclear →  
`HELP NEEDED: Authority compromised [reason]`.

------------------------------------------------------------
1. SYSTEM STRUCTURE
------------------------------------------------------------
Root folders:
- /skills     (instructions only)
- /data       (reference only)
- /projects   (active work)

No Skills/Data inside project subfolders.
No additional README.md files anywhere.

------------------------------------------------------------
2. STATE & STRATEGY
------------------------------------------------------------
UPDATE_STRATEGY = 1
1 = Build (staging, growth mode)
2 = Read-only (validate, route; no writes)

If =2 → ignore Sections 5–7 entirely.

------------------------------------------------------------
3. REGISTRY (CID)
------------------------------------------------------------
CID   Name     Type   Purpose                        Path
R01   README   Skill  System Authority & Routing     README.md

Rules:
- Skills/Data may reference R01
- **Only main project file has CID**
- Sub-files must NOT contain CID entries

------------------------------------------------------------
4. ROUTING (if STRATEGY=1)
------------------------------------------------------------
If target exists → run its Execution Prompt.
If missing:
- ask user scope (micro-question only)
- propose filename: {purpose}-{type}.md
- stage file + registry entry in *_update.md

If mismatch/ambiguity → STOP protocol.

------------------------------------------------------------
5. FOLDER RULES (if STRATEGY=1)
------------------------------------------------------------
/                ← only this README.md
/skills/         ← execution instructions
/data/           ← reference; no execution
/projects/{x}/   ← project index + subfiles; no CIDs in subfiles

------------------------------------------------------------
6. SELF-HEAL (if STRATEGY=1)
------------------------------------------------------------
- Fix Execution Prompt if verbose
- If missing registry path → search, propose patch
- If unresolved → STOP protocol

------------------------------------------------------------
7. FILE TEMPLATE (MIN)
------------------------------------------------------------
# {Filename}

{{IF SKILL OR PROJECT}}
Metadata: [Type: {{Skill|Project}} | CID: {{ID}}]

## Execution Prompt (Local Authority)
Authority: README.md (R01)
Action: {{verb}} {{object}}
Output: {{filename}}_update.md
Rules: markdown only; no direct overwrite.
{{END}}

{{IF DATA}}
## Authority
Type: Data (reference-only; no execution logic)
{{END}}

## Purpose
Short one-line intent.

## Content
Essential logic / SOP / decision chain (≤20 lines).

## Notes
Optional.

{{IF PROJECT}}
## Index
Path | Purpose | By
:--- | :------ | :--
{{path}} | {{desc}} | AI/User
{{END}}

------------------------------------------------------------
END OF SPEC (V1.3_MIN)
------------------------------------------------------------
