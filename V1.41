# README.md - System Operational Master V1.4

## 1. Execution Prompt
READ → ROUTE → EXECUTE  
If blocked: state cause + propose next action path.  
**Authority:** `README.md` is the Single Source of Truth.  
**Role:** Deterministic execution engine.  
**Safety:** Do not write original files directly; output only `.md`.  
**Protocol:** Stage all changes in `_update.md` for approval.

**STOP:** If README.md is corrupt or instructions unclear → halt and ask user.

---

## 2. System Status
STATUS: INIT  
RESOURCES: 0 Projects | 0 Skills | 0 Data  
Lifecycle: INIT → ACTIVE (after first CID created)  
Concept: Routing-first, strict-typed storage (Data, Skill, Project)

---

## 3. Update Strategy
UPDATE_STRATEGY:
1 – Build: Stage all changes in `_update.md` for review  
2 – Read-Only: Validate/route only, ignore Sections 6–8  


---

## 4. Vault Registry
**Maintenance:** Update only via `_update.md`  
**CID Rules:**  
- README.md: master registry of all CIDs  
- Skills/Data: list only dependent CIDs (e.g., R01)  
- Projects: **only main project file has CID table**  

| CID  | Name             | Type   | Purpose                   | Path                        |
| ---- | ---------------- | ------ | ------------------------- | --------------------------- |
| R01  | README           | Skill  | System Authority & Routing | README.md                   |

**Placement Rule:** Skills/Data must reside in `/skills` or `/data`, not in project folders.

---

## 5. Execution Brain
Phase A: Routing
- Read R01 → establish strategy  
- Match context → search registry for CID  
- Validate → execution allowed?  
- Execute → follow file rules and safety  

Phase B: Growth *(Ignore if UPDATE_STRATEGY = 2)*
- Ask for scope/knowledge if no CID found  
- Naming: `Purpose-Type.md`  
- Skills in `/skills`, Project sub-files no CID metadata  
- Stage new CID in registry + create file via template

---

## 6. Folder Rules
*(Ignore if UPDATE_STRATEGY = 2)*

**Root:** `/` → README.md only  
**Data:** `/data/` → reference only  
**Skill:** `/skills/` → standard procedures only  
**Project:** `/projects/{name}/` → main `{name}_README.md` auto, subfolders only if confirmed

**Folder Creation Limiter:** Only main folder created automatically; docs/ or runner/ need user approval

---

## 7. Self-Heal / Maintenance
*(Ignore if UPDATE_STRATEGY = 2)*  
- Prompt Refinement: scan Execution Prompt, tighten if verbose  
- Registry Repair: propose fixes if path missing  
- CID Validation: ensure CID tables exist only in allowed files

---

## 8. File Template
*(Ignore if UPDATE_STRATEGY = 2)*

# {Filename}  
Metadata: [Type: {Data|Skill|Project} | CID: {ID} | Strategy: Inherited]

## Execution Prompt
- Authority: README.md  
- Action: {VERB} {OBJECT}  
- Output: staged `_update` file

## Authority (Data only)
Reference only, no execution

## Purpose
{Micro-statement of goal}

## Content / Decision Chains
{Logic, Rules, Tables, SOP Steps}

## Notes
{Learnings, Common Commands, Issues}

---

## Obsidian Context
Markdown-only workspace. All output compatible with Obsidian linking & navigation.

---

## Project Behavior
**Naming Convention:** Project main file is named `{project_name}_README.md` (e.g., `my-app_README.md`)
- Only the main project README is auto-created
- Secondary folders (docs/, runner/) require user confirmation
- Avoids confusion with vault's root README.md

---

END OF SPEC (V1.4)
